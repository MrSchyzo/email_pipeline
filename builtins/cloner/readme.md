# Simple copier plugin

Copies files to dedicated folders according to the given matching rules.
Matching rules are defined in the config file which consists of a list of rules definitions: see [config.example.json](config.example.json) as an example.