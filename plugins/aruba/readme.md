# Bollo plugin

Just copies any attached PDF at `<DST_ROOT>/{YYYYMMDD}_fattura_{receipt_num}.pdf` if `La tua fattura per gli ordini di Aruba` is in the subject.
`DST_ROOT` can be specified in the `.env` file.